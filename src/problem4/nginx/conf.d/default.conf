server {
    listen 80;

    location = / {
        default_type text/plain;
        return 200 "Welcome to the platform\n";
    }

    location /api/ {
        proxy_pass http://api:3000;
    }

    location /status {
        proxy_pass http://api:3000;
    }
}